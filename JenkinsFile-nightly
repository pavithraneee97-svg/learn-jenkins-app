pipeline {
    agent any

    stages {

         stage('docker') {

            steps {
                sh 'docker build -f ci/Dockerfile-dockercli -t my-docker-cli .'
                sh 'docker build -f ci/Dockerfile-awscli -t my-aws-cli .'
                sh 'docker build -f ci/Dockefile-playwright -t my-playwright .'
            }

         }

    }

}